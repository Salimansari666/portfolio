version: "3.8"
services:
  backend:
    build: .
    ports:
      - "8000:8000"
    environment:
      - HF_TOKEN=${HF_TOKEN}
      - API_KEY=${API_KEY}
    volumes:
      - ./:/app
    restart: unless-stopped
